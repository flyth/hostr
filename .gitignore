.DS_Store
.idea
bin/
data/
node_modules/
frontend/build/
frontend/.svelte-kit/

# The embedded SPA is generated by `make frontend`; only the fallback page is
# tracked so `go build` works on a fresh clone without Node.
web/*
!web/placeholder.html

# stray binary from a bare `go build`
/hostr
/hostrctl
